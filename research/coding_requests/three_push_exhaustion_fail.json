{
  "family": "three_push_exhaustion_fail",
  "template": "novel",
  "clock": "4h/4h",
  "side": "BOTH",
  "needs_feed": false,
  "needs_new_indicator": false,
  "novel_reason": "Three-push exhaustion fail as one 4h BOTH family (SHORT priority). Garwe geometry SUPERSEDES looser Munha fail-to-extend OR. Pushes live on t-3, t-2, t-1. SHORT: high[t-3] < high[t-2] < high[t-1] AND both successive advances >= min_push_atr*ATR20 AND high[t] <= high[t-1] AND close[t] < open[t] AND close[t] < close[t-1]. LONG: inverse (three falling lows then fail new low + strong close). Exactly 3 prior pushes (n_pushes not searched). Size gate >= (exact min_push_atr*ATR counts). Two-sided fail prints SHORT, not LONG. ATR period locked 20, known before the signal bar (atr.shift(1)). Fill t+1 open. No volume/session/VP gate. Free search (1 only): min_push_atr [0.15, 0.35] (endpoints only; do NOT use [0.3, 0.6]). Not consecutive_bar_exhaustion. Not failed_higher_high. Not swing_break_fail_reversion (family I / Job 153). Not swing_failure_reversal. Not key_reversal_bar (family H / Job 152 — bar t breaks t-1). Not thrust_bar_fail_reversion (family G / Job 151). Not inside_bar_break_fail (family F). Not outside_bar_reversal / outside_bar_fail_reversion. Not three_white_soldiers / three_black_crows / three_bar_play. Not expansion_fail_fade / candle_reject_reversal. Not dead VP (prior_poc / hvn / lvn / rolling VA). Not Job 133 bullish_rectangle_fail_reclaim (DEAD 0/12). Not keltner_channel_fade (Job 144 0/12). Not utc_open_fail_reversion (0/12). Do not recode spent families 118–153. Do not revive Job 133 rectangle. Do not modify sibling geometry.",
  "summary": "Fade a 4h three-push exhaustion that fails a 4th extreme and prints a strong reverse close (SHORT = 3 sized HH then fail+bear close; LONG = 3 sized LL then fail+bull close) as one 4h BOTH family with SHORT priority.",
  "justification": "A locked ATR20 exact-three prior bar-extreme push (n_pushes=3) sized by min_push_atr (>=) then fail-new-extreme AND strong reverse close, with searched min_push_atr [0.15, 0.35] only (not [0.3, 0.6]), is not a consecutive-close fade, not a confirmed-pivot failed HH, not a lookback-N swing wick-fail, not a t-1 key-reversal break, not a thrust/IB/outside clone, not a dead VP cluster, and not Job 133 rectangle / other finished 0/12 clones. Garwe strong-close conjuncts supersede looser Munha fail-to-extend-OR.",
  "written_at": "2026-09-17T00:00:00+00:00",
  "owner": "sleeve_engineer",
  "instruction": "Write core/strategy/three_push_exhaustion_fail.py implementing Strategy. Use only bars <= t. Fill at t+1 open. Add a lookahead test. Register via class name. Do not LLM-dump an unverified file. Do not start walk-forward. Do not call mark_done."
}
